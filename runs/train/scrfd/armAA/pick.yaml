# PII-1379. rule is last_epoch for every arm until the per-epoch sweep
# (PII-1372) replaces it. No eval number is recorded here.
arm: armAA
family: scrfd
rule: last_epoch
epoch: 20
epoch_unit: epoch
status: deprecated
wandb_tags: [deprecated]
onnx: onnx/armAA_det10g.onnx
checkpoints: local
remote_wd: shang:/data/esteban/pii/runs/train/wd_armAA
local_epochs: 20
epoch_evidence: >-
  pii_train/export_armAA_onnx.py CKPT = wd_armAA/epoch_20.pth; PII-1311 export_epoch.py
  asserts that same recorded CKPT
notes: >-
  Trained on shang. epoch_20.pth was scp'd here early into wd_armAA_ckpt; epochs 1..19,
  latest.pth, the config and the logs were pulled from shang:runs/train/wd_armAA/ by PII-1447
  (shang keeps its copy); the epoch_20.pth already here was asserted byte identical to
  shang's, not overwritten.
