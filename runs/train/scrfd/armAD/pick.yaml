# PII-1379. rule is last_epoch for every arm until the per-epoch sweep
# (PII-1372) replaces it. No eval number is recorded here.
arm: armAD
family: scrfd
rule: last_epoch
epoch: 20
epoch_unit: epoch
status: deprecated
wandb_tags: [deprecated]
onnx: onnx/armAD_det10g.onnx
checkpoints: local
remote_wd: shang:/data/esteban/pii/runs/train/wd_armAD
local_epochs: 20
epoch_evidence: >-
  PII-1311 / PII-1336 export_epoch.py asserts the recorded exporter's CKPT ==
  wd_armAD/epoch_20.pth
notes: >-
  All 20 epochs, latest.pth, the config and the logs were pulled from
  shang:runs/train/wd_armAD/ by PII-1447; shang keeps its copy.
