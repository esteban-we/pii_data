# PII-1379. rule is last_epoch for every arm until the per-epoch sweep
# (PII-1372) replaces it. No eval number is recorded here.
arm: armAC
family: scrfd
rule: last_epoch
epoch: 20
epoch_unit: epoch
status: active
wandb_tags: [10g-ablations]
onnx: onnx/armAC_det10g.onnx
checkpoints: local (epoch 20 only) and shang
local_epochs: 1
epoch_evidence: >-
  PII-1311 / PII-1336 export_epoch.py asserts the recorded exporter's CKPT ==
  wd_armAC/epoch_20.pth; PII-1349 re-exported the same checkpoint here and reproduced the
  recorded armAC row
notes: >-
  Trained on shang. Only epoch_20.pth was scp'd here (wd_armAC_ckpt); epochs 1..19 are on
  shang in runs/train/wd_armAC/.
